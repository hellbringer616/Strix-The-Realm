Skills: [
    {
        Id: "craft:bronzebar"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 1, 2) && Itemizer.hasItemizerItem(player, 1, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 7.5 metallurgy metallurgy");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[1, 2], [2, 2]], [21, 1]);

            """
    }
    {
        Id: "craft:bronzepick"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 18 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 2]], [1000, 1]);
            """
    }
    {
        Id: "craft:bronzedagger"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 18 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 2]], [1300, 1]);
            """
    }
    {
        Id: "craft:bronzemace"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 3)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 27 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 3]], [1200, 1]);
            """
    }
    {
        Id: "craft:bronzelongsword"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 36 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 4]], [1100, 1]);
            """
    }
    {
        Id: "craft:bronzehatchet"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 18 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 2]], [1020, 1]);
            """
    }
    {
        Id: "craft:bronzesabatons"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 36 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 4]], [9100, 1]);
            """
    }
    {
        Id: "craft:bronzehelm"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 5)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 45 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 5]], [9101, 1]);
            """
    }
    {
        Id: "craft:bronzelegs"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 21, 7)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 63 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[21, 7]], [9102, 1]);
            """
    }
    {
        Id: "craft:bronzeplate"
        Parent: active
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
            var exp = 72
            var class = "armorer"
            var requiredItem1 = 21
            var requiredItem1Qty = 8
            var craftedItem = 9103
            var craftedItemQty = 1
            processItemizerItemCost(_caster, requiredItem1, requiredItem1Qty, exp, class, [[requiredItem1, requiredItem1Qty]], [craftedItem, craftedItemQty]);
            """
    }
]
