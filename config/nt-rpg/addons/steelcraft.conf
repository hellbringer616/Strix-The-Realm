Skills: [
    {
        Id: "craft:steelbar"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 3, 2) && Itemizer.hasItemizerItem(player, 4, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 22.5 metallurgy metallurgy");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[3, 2], [4, 4]], [23, 1]);
            """
    }
    {
        Id: "craft:steelpick"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 54 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 2]], [1002, 1]);
            """
    }
    {
        Id: "craft:steeldagger"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 54 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 2]], [1302, 1]);
            """
    }
    {
        Id: "craft:steelmace"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 3)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 81 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 3]], [1202, 1]);
            """
    }
    {
        Id: "craft:steellongsword"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 108 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 4]], [1102, 1]);
            """
    }
    {
        Id: "craft:steelhatchet"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 2)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 54 weaponsmith weaponsmith");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 2]], [1022, 1]);
            """
    }
    {
        Id: "craft:steelsabatons"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 4)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 108 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 4]], [9120, 1]);
            """
    }
    {
        Id: "craft:steelhelm"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 5)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 135 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 5]], [9121, 1]);
            """
    }
    {
        Id: "craft:steellegs"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 7)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 189 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 7]], [9122, 1]);
            """
    }
    {
        Id: "craft:steelplate"
        Parent: active
        Skill-Types: [
            "ntrpg:lightning"
        ]
        Settings: {
            "mana": 0
            "mana_levelbonus": 0
        }
        Loader: js
        Script:
            """
                var player = _caster.getEntity();
                var playerName = _caster.getPlayer().getName()
                //Tests begin here. Assume that player inventory is empty

                if(Itemizer.hasItemizerItem(player, 23, 8)) {
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "nadmin exp add " + playerName + " 216 armorer armorer");
                }else{
                  Sponge.getCommandManager().process(Sponge.getServer().getConsole(), "tell " + playerName + " You do not have the required items");
                }
                Itemizer.craft(player, [[23, 8]], [9123, 1]);
            """
    }
]
